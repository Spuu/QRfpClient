#include "product.h"

Product::Product(std::string name, std::string ean,
                 double price, double quantity) :
    name(name),
    ean(ean),
    price(price),
    quantity(quantity)
{

}

